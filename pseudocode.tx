//Create function for submit button
function checker(){

}
//Inside the function initialize the variable that gets the user input
let input = user input

//Initialize the variable for that selects HTML element
let h2 = document.querySelector("h2") ;

//Create a loop to check the matching letters
for(let i=0;)

//Initiaze the viaraible that gets the form 
let form = document.getElementById

//Add an event listener for the button and call the fuction to run
