//Initialize variable for user input
SET input = HTML element
SET h2 = HTML element(h2)

// Create function for submit button
CALL function checker(){
}

//SET loop
index = zero
index is LESS THAN userInput.length
ADD 1 each time

//Inside LOOP block
h2.innerHHTML = userInput.value

//SET variable that gets form
form = get HTML element  by Inside
ADD event Listener and call checker function
